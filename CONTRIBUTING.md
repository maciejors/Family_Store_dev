- Running the app: `npm run dev`
- Running all tests: `npm test`

## Translations:

- Messages can be found in [`src/i18n/messages`](src/i18n/messages/)
- For `next-intl` to work in tests, we unfortunately cannot use async components

## Supabase:

See [`src/lib/supabase/README.md`](src/lib/supabase/README.md)
